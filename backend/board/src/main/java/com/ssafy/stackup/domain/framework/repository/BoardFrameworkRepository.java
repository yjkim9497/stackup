package com.ssafy.stackup.domain.framework.repository;

import com.ssafy.stackup.domain.board.entity.BoardFramework;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardFrameworkRepository extends JpaRepository<BoardFramework, Long> {
}
